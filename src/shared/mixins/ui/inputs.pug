mixin calendar
  input.xo-inputs__calendar(type='date' name='xo-date' id='xo-date')

mixin simple_input(placeholder, width)
  input.xo-inputs__simple-input(type='text' placeholder=placeholder name='xo-text' id='xo-text' style=`width: ${width}px`)

mixin simple_select(options = [], width)
  select.xo-inputs__simple-select(style= `width: ${width}px` name='xo-select' id='xo-select')
    each option in options
      - var [key, val] = Object.entries(option)[0];
      option(value= key)
        | #{val}
